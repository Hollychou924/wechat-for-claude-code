{
  "name": "wechat-for-claude-code",
  "version": "0.3.0",
  "description": "Bridge WeChat messages to Claude Code — use Claude Code directly from WeChat",
  "type": "module",
  "scripts": {
    "setup": "bun setup.ts",
    "start": "bun src/index.ts",
    "status": "bun scripts/status.ts",
    "uninstall": "bun scripts/uninstall.ts"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "license": "MIT"
}
